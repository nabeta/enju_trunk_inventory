<%= form_for(@inventory_shelf_group) do |f| %>
  <% if @inventory_shelf_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_shelf_group.errors.count, "error") %> prohibited this inventory_shelf_group from being saved:</h2>

      <ul>
      <% @inventory_shelf_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :display_name %><br />
    <%= f.text_field :display_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
